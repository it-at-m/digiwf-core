

export const getPresignedUrlForFileUploadFromTaskservice = (cfg: any, formContextId: string, fileName: string, filePath: string) => {
  // FIXME: fill with content
  return Promise.resolve("url")
}
