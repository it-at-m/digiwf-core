<jaxws:bindings
        xmlns="https://jakarta.ee/xml/ns/jaxws"
        xmlns:jaxws="https://jakarta.ee/xml/ns/jaxws"
        wsdlLocation="fabasoft.wsdl">
    <!-- Makes sure that the WebPort methods receive one object instead of a list of individual properties -->
    <enableWrapperStyle>false</enableWrapperStyle>
</jaxws:bindings>