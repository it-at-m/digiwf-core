de:
  muenchen:
    oss:
      digiwf:
        okewo:
          password: "password"
          url: "http://localhost:8189"
          username: "username"
          benutzerId: "benutzerId"
spring:
  cloud:
    stream:
      bindings:
        functionRouter-in-0:
          destination: "dwf-address-test"

logging:
  level:
    org.apache.kafka.clients.admin: WARN
    org.apache.kafka.clients.consumer: WARN
    org.apache.kafka.clients.producer: WARN
