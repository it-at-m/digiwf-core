spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
          consumerProperties:
            auto.offset.reset: latest
          configuration:
            security:
              #protocol: SSL
