id: assign_cleanup_definitions
author: Simon Zambrovski
realm: ${SSO_REALM}
changes:
  - addRole:
      clientId: ${SSO_ENGINE_CLIENT_ID}
      clientRole: true
      name: admin
      description: Special process definitions cleanup role.
  - assignRole:
      user: johndoe
      clientId: ${SSO_ENGINE_CLIENT_ID}
      role: admin
  - assignRoleToClient:
      roleClientId: ${SSO_ENGINE_CLIENT_ID}
      role: admin
      clientId: ${SSO_TASK_CLIENT_ID}
