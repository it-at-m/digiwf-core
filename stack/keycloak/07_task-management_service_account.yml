id: task-management_service_account
author: Simon Zambrovski
realm: ${SSO_REALM}
changes:
  - updateClient:
      clientId: ${SSO_TASK_CLIENT_ID}
      serviceAccountsEnabled: true
  - assignRoleToClient:
      clientId: ${SSO_TASK_CLIENT_ID}
      roleClientId: ${SSO_ENGINE_CLIENT_ID}
      role: clientrole_taskuser
  - assignRoleToClient:
      clientId: ${SSO_TASK_CLIENT_ID}
      roleClientId: ${SSO_ENGINE_CLIENT_ID}
      role: BACKEND_DEPLOY_RESOURCE

