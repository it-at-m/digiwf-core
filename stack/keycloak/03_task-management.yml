id: task-management
author: Simon Zambrovski
realm: ${KEYCLOAK_REALM:-localrealm}
changes:
  - addSimpleClient:
      clientId: ${TASK_CLIENT_ID}
      redirectUris:
        - http://localhost:38765/*